# Research: userguide

_Auto-captured from the 2026-09-02 research workflow._

## Summary

There is no step-by-step illustrated user guide for A-Z+T, and there is not a single screenshot of the running app anywhere in the repo. `/Users/Seth/GIT/azt/docs/` holds 37 markdown files, but they split into conceptual/rationale prose (RATIONALE, OWL_GUIDE, WHYCOMPUTERS, WHYCOMMUNITIES), task reference lists (TASKS, TASKSCOLLECTION, TASKSANALYSIS — ~40 lines each, no procedures), install docs (INSTALL, SIMPLEINSTALL*), and one long narrative reference (USAGE.md, 221 lines, no numbered steps, and Kent's own CHANGELOG lists "rework USAGE.md" under "#Next up:"). Every PNG in `/Users/Seth/GIT/azt/images/` is Kimberly Rasmussen's icon/theme art (250x585 card-stack glyphs etc.), and the `docs/*.png|svg` files are hand-authored conceptual diagrams, not UI captures. `docs/sorting_workflow.svg` is the closest existing artifact — it already narrates the *vowel sorting* path "screen by screen" — so the genuine gap is (a) the wordlist-collection path, which no document covers procedurally, and (b) real screenshots. Running the app for screenshots is entirely feasible on Seth's Mac (python3.11.8 + tkinter 8.6 + Charis v7 + PyAudio with live devices are all present; only `kivy` is missing from the import gate) but the first `python3 main.py` will auto-create a venv and `pip install -r requirements.txt` (torch 2.7.1+cpu, openai_whisper, transformers, kivy, opencv — multi-GB) plus git-clone the `images_CAWL` sister repo ("a few hundred MB"). Headless cloud capture is technically conceivable (Xvfb + Tk) but is blocked in practice by that dependency weight plus three `wait=True` modal dialogs at boot (bootstrap/sound/font) that stall an unattended run, and the webview backend is not a shortcut (no `Text` widget, stub `Notebook`/`ToolTip`, and `pywebview` is not even in requirements.txt). Along the way I found a live defect exactly on the wordlist-collection path: the "Add remaining CAWL entries" button silently fails to render for a brand-new database because three copies of its tooltip use `"{}"` with `.format(count=...)`, raising IndexError that `ui_shell.finalbuttons` swallows. Recommendation: a cloud session writes 100% of the prose, the structure, and new SVG diagrams in the existing house style, plus a numbered shot-list with exact preconditions; Seth's Mac (or a local Claude Code session with `screencapture`) supplies the PNGs.

## Findings

- **[high]** No step-by-step illustrated user guide exists; the closest thing is USAGE.md, which is narrative reference prose with zero numbered procedures and no UI screenshots.
  - _evidence:_ /Users/Seth/GIT/azt/docs/USAGE.md (221 lines). Its only images are three icon references (L128 `../images/T alone clear6_icon.png`, L133 `../images/Microphone alone_sm.png`, L131 `AZT Process Flow Chart.png`). Headings are topical ('Settings File', 'The Main Window', 'Tone Reports'), never 'Step 1'.
  - _implication:_ A step-by-step guide is a genuine new artifact, not a rewrite. Nothing to duplicate.

- **[high]** Kent himself has flagged USAGE.md as needing rework, so contributing a replacement/companion guide upstream is pre-sanctioned.
  - _evidence:_ /Users/Seth/GIT/azt/docs/CHANGELOG.md:10 — under `#Next up:` the first bullet is `- rework USAGE.md`; L15 adds '- Clarity for user wrt what is data (i.e. lc, pl, imp), and what is analysis (i.e., lx, ps) —in the presentation of the task itself, and in the documentation'.
  - _implication:_ Frame the PR as fulfilling Kent's own roadmap item; lead the guide with the data-vs-analysis distinction he asked for.

- **[high]** TASKSCOLLECTION.md is the only doc covering wordlist collection, and it is ~45 lines of description with no procedure and no images.
  - _evidence:_ /Users/Seth/GIT/azt/docs/TASKSCOLLECTION.md — 45 lines total; 'Add Words' section is L6-15, 'Record Words' L17-18. No screenshots, no click sequence.
  - _implication:_ This is the exact gap Seth wants filled. The new guide should link back to it rather than restate it.

- **[high]** TASKSCOLLECTION.md documents an 'Add Words' task that the shipped Task Chooser no longer offers — the non-audio collection variants are commented out.
  - _evidence:_ /Users/Seth/GIT/azt/tasks/chooser.py:177-182 — `tasks=[` then `# WordCollectionCitation,` (L178), `WordCollectionCitationwRecordings,` (L179), `# WordCollectnParse,` (L180), `WordCollectnParsewRecordings,` (L181), `RecordCitation` (L182). Task titles: 'Add Words' = WordCollectionCitation (tasks/tasks.py:452), 'Add Words with Audio' = WordCollectionCitationwRecordings (tasks.py:463), 'Add and Parse Words with Audio' = WordCollectnParsewRecordings (tasks.py:598).
  - _implication:_ The guide must document the AUDIO-first collection path ('Add Words with Audio' / 'Add and Parse Words with Audio'), not the doc's 'Add Words'. Screenshots taken from the current build will not match TASKSCOLLECTION.md.

- **[high]** DEFECT on the wordlist-collection path: the 'Add remaining CAWL entries' button silently disappears for a new database, because its tooltip string uses `{}` with a keyword-only .format().
  - _evidence:_ `"(you are missing {} CAWL tags)" ... .format(count=len(...))` at /Users/Seth/GIT/azt/backend/core/lexicon.py:731, /Users/Seth/GIT/azt/tasks/tasks.py:578, /Users/Seth/GIT/azt/tasks/tasks.py:664. Verified: `s.format(count=3)` -> `IndexError: Replacement index 0 out of range for positional args tuple`. Correct form exists at tasks.py:537 (`{count}`). Caller /Users/Seth/GIT/azt/frontend/ui_shell.py:1100 `kwargs=self.program.task.dobuttonkwargs()` is wrapped in `except Exception as e: log.error(f"Problem: {e}")` (ui_shell.py:1103-1104), so the button is never built and nothing is shown to the user. Both msgid variants exist in /Users/Seth/GIT/azt/translations/azt.pot (L11119 area), confirming the broken string is real source text.
  - _implication:_ Affects exactly the tasks the chooser offers (WordCollectionCitationwRecordings via lexicon.py:731; WordCollectnParsewRecordings via tasks.py:578) and only when `cawlmissing` is non-empty, i.e. a brand-new language project. A trivial, high-value first PR to rulingAnts->kent-rasmussen. Reachability inferred from code, not executed.

- **[high]** USAGE.md contains at least two hard factual errors against the current code.
  - _evidence:_ (1) /Users/Seth/GIT/azt/docs/USAGE.md:88 'stores this location in `lift_url.py` ... delete `lift_url.py`' — but the path is now stored via app settings: /Users/Seth/GIT/azt/utilities/file.py:270 `def writefilename(filename='')` -> `mgr = _app_settings(); mgr.filename = filename`; `lift_url.py` survives only as a log string (backend/core/file_parser.py:21) and a VCS ignore entry (backend/core/vcs.py:801). (2) docs/USAGE.md:152 '[A-Z+T] doesn't do CV sorting and verification (Yet!)' — SortV/SortC exist (tasks/tasks.py:1236, 1247) and are offered by the chooser (tasks/chooser.py:186-187), and TASKSCOLLECTION.md L22-30 documents them.
  - _implication:_ Do not copy USAGE.md prose into the new guide without checking it against source. Both fixes are cheap upstream contributions.

- **[high]** INSTALL.md's dependency claim is badly stale and omits the automatic venv+pip bootstrap that now runs on every start.
  - _evidence:_ /Users/Seth/GIT/azt/docs/INSTALL.md:4 'You need Python 3+ and _one_ dependency ([PyAudio]) to run [A-Z+T]'. Actual: /Users/Seth/GIT/azt/requirements.txt lists ~30 packages including `torch==2.7.1+cpu` (L47), `openai_whisper` (L32), `transformers` (L49), `kivy` (L67), `opencv-python` (L22), `faster_whisper` (L14). /Users/Seth/GIT/azt/utilities/py_modules.py calls `ensure_venv()` at L455 and `sync_requirements()` at L518 — both at import time, reached from main.py:16 `import utilities.py_modules`.
  - _implication:_ Any install section in the new guide must be written from requirements.txt + py_modules.py, not from INSTALL.md. This is also the single biggest blocker to headless capture.

- **[high]** Launching the app for the first time in this clone will create a venv and run a multi-GB pip install, and will git-clone a sister repo of several hundred MB.
  - _evidence:_ utilities/py_modules.py:304 `ensure_venv()` docstring: 'SISTER (<azt>/../env ...) then CHILD (<azt>/env); if neither exists, CREATE the child' then relaunches (L437-455). `sync_requirements()` (L457-518) runs `pip install -f modulestoinstall -r requirements.txt` when the venv stamp mismatches; `/Users/Seth/GIT/azt/modulestoinstall/` contains only a placeholder file, so the offline pass fails and it goes online. `ensure_sister_repos()` (py_modules.py ~L520) clones per /Users/Seth/GIT/azt/utilities/sister_repos.py:56-60 `images_CAWL` with `size_note=_("(the CAWL image set is a few hundred MB; the first clone can take a while)")`. Neither /Users/Seth/GIT/env nor /Users/Seth/GIT/azt/env exists; /Users/Seth/GIT/images_CAWL, /Users/Seth/GIT/lift_templates, /Users/Seth/GIT/azt-collab do not exist.
  - _implication:_ Do not casually run `python3 main.py` from an agent session — it mutates the working tree and downloads GBs on Papua bandwidth. Pre-stage deliberately.

- **[high]** On Seth's Mac the app is one package away from running with NO venv and NO heavy install: every module the import gate checks is already present in system python except kivy.
  - _evidence:_ Import gate at /Users/Seth/GIT/azt/utilities/py_modules.py:521-560 imports urllib3, numpy, pyaudio, PIL, lxml, psutil, soundfile, scipy, transformers, huggingface_hub, langcodes, whisper, patiencediff, reportlab, language_data, svglib, torch and does `find_spec('kivy')`. Probe of /Library/Frameworks/Python.framework/Versions/3.11 (python3 = 3.11.8): all OK except `kivy MISSING` (also missing but NOT gate-checked: faster_whisper, dulwich, zeroconf, samplerate). Failure of that gate calls `pip_install()` (py_modules.py:574) which installs its whole hardcoded list into whatever interpreter is running.
  - _implication:_ Fastest safe path to screenshots on the Mac: `python3 -m pip install kivy` then `AZT_NO_VENV=1 python3 main.py` from /Users/Seth/GIT/azt. With AZT_NO_VENV=1, sync_requirements() returns early (py_modules.py:465-469 'only manage a venv, never a system python'), so the system's torch 2.10.0 is NOT downgraded to the pinned 2.7.1+cpu. Seth must decide whether kivy-in-system-python is acceptable; the supported alternative is `python3 -m venv env && source env/bin/activate && pip install -r requirements.txt && python main.py` (matches /Users/Seth/GIT/azt/CLAUDE.md L12-20).

- **[high]** The Mac already satisfies the three startup checks that would otherwise raise BLOCKING modal dialogs.
  - _evidence:_ Blocking notices at /Users/Seth/GIT/azt/main.py:506 warn_bootstrap_problems (ErrorNotice wait=True, L526), main.py:478 warn_sound_problems (`ErrorNotice(..., wait=True) #blocking: acknowledge before any work`, L504-505), main.py:528 warn_font_problems (`ErrorNotice(..., wait=True)`, L547) — all called in `_run_setup` (main.py:571-573). Mac probe: tkinter 8.6 OK; pyaudio reports 7 devices, default in 'MacBook Pro Microphone', default out 'MacBook Pro Speakers'; Tk font families include 'Charis' — which /Users/Seth/GIT/azt/utilities/fonts.py:23 accepts (`CHARIS_FAMILIES = ['Charis SIL', 'Charis']`, Charis v7 naming).
  - _implication:_ An unattended screenshot run will not stall on Seth's Mac. On a bare Linux container all three would fire and block, since portaudio and Charis SIL are absent by default.

- **[high]** Seth's clone is automatically in 'production' mode — the mode Kent annotates as the one for making screenshots.
  - _evidence:_ /Users/Seth/GIT/azt/main.py:19 `'production':False, #True for making screenshots (default theme)`; main.py:1158 `if self.aztdir.parent.stem == 'AZT':` (dev branch) `else:` main.py:1170 `self.production=True #True for making screenshots (default theme)`. /Users/Seth/GIT/azt has parent stem 'GIT', so the else branch applies. check_for_theme (main.py:1016-1023) then leaves the default theme unless a theme file exists.
  - _implication:_ Screenshots taken on Seth's Mac out of the box will already use the stock default theme, i.e. what other users see. No special setup needed.

- **[high]** A dev-only auto-start hook exists that would let a screenshot harness skip the database chooser and jump straight into a named task — but only if the clone's parent directory is named 'AZT'.
  - _evidence:_ /Users/Seth/GIT/azt/main.py:1158-1166: under `aztdir.parent.stem == 'AZT'` it sets `testing=True`, `testlift='Demo_en'`, `testtask='SortT'`, with commented alternatives including `# self.testtask='WordCollectnParsewRecordings'`. Consumed at main.py:468 (`if self.testing and (tl:=getattr(self,'testlift',None))` selects the matching LIFT and returns, skipping askwhichlift) and tasks/chooser.py:132-133 (`if self.program.testing and hasattr(self.program,'testtask'): self.maketask(self.program.testtask)`).
  - _implication:_ For a repeatable screenshot run, work from a second checkout at e.g. ~/AZT/azt and set testtask per shot. Caveat: testing=True also 'eliminates Error screens and zipped logs and repo commits' (main.py:1159), so error-state screenshots would differ from a real user's.

- **[high]** The demo database is the WRONG starting state for a wordlist-collection guide; 'Start work on a new language' is the right one.
  - _evidence:_ Demo flow: frontend/ui_shell.py:3276 `makeCAWLdemo` -> `templates.CAWL(self.program,demo=True)` -> loadCAWL of the full SILCAWL -> `demo.convertglosstocitation(self.demolang)` (ui_shell.py:3323), which fills citation forms from glosses (io_put/lift.py:2331-2340). The 'Add remaining CAWL entries' button only appears when `self.program.taskchooser.cawlmissing` is non-empty (tasks/chooser.py:297-306 getcawlmissing scans all 1700 tags). New-language flow: LiftChooser option `('New',_("Start work on a new language"))` (ui_shell.py:3421) -> `startnewfile()` (ui_shell.py:2982) -> language-code entry page with a live-filtered list of possibles (`make_lang_entry` L2993, `_show_possibles` L3020).
  - _implication:_ Guide's main spine = New language (type 'fau'/Fayu) -> Add remaining CAWL entries -> type citation forms one at a time. Use the Demo DB only for the later sorting/reporting chapters, exactly as docs/DEMOS.md intends.

- **[high]** SILCAWL is not in this clone and must be fetched before any wordlist collection or demo can happen; the CAWL illustration set is a separate several-hundred-MB fetch that can be suppressed.
  - _evidence:_ /Users/Seth/GIT/azt/lift_templates/ contains only SILCAWL_ReadMe.md, SILCAWL_update.py, __init__.py — no SILCAWL/ symlink (it is gitignored: .gitignore `lift_templates/SILCAWL`). io_put/cawl.py:11-14 `stockCAWL=file.pathname_from_base_dir('lift_templates/SILCAWL/SILCAWL.lift')`; if absent, `from lift_templates.SILCAWL_update import ensure_available; ensure_available()` -> sister_repos.ensure('lift_templates') clones https://github.com/kent-rasmussen/lift_templates.git to /Users/Seth/GIT/lift_templates and symlinks it in. Images: io_put/lift.py:3163-3165 `rootimgdir='images/toselect/'; if not os.path.isdir(rootimgdir): from images.to_select_update import ensure_available; ensure_available()`. sister_repos.available() short-circuits on `os.path.isdir(linkpath)` (utilities/sister_repos.py:114-116). Confirmed live: /Users/Seth/GIT/azt/userlogs/log_2026-09-01.txt records 'images_CAWL not found; cloning ... to /Users/Seth/GIT/images_CAWL' with the size note.
  - _implication:_ Pre-stage: `git clone https://github.com/kent-rasmussen/lift_templates.git /Users/Seth/GIT/lift_templates` (small, required). For images: either clone images_CAWL (needed if the guide shows the CAWL pictures) or `mkdir -p /Users/Seth/GIT/azt/images/toselect` to suppress the download at the cost of no illustrations. Note `rootimgdir` is RELATIVE, so the app must be launched with cwd = /Users/Seth/GIT/azt.

- **[high]** There are zero screenshots of the A-Z+T UI in the repo. All 90+ files in images/ are hand-drawn icon art; docs/*.png|svg are conceptual diagrams.
  - _evidence:_ /Users/Seth/GIT/azt/images/README.md: 'All images in this folder were made by Kimberly Rasmussen.' Dimensions confirm iconography: `images/Sort List.png` 250x585, `images/Join List.png` 231x484, `images/AZT stacks6.png` 500x459; visual read of Sort List.png shows abstract striped card-stacks, not UI. docs/ PNGs are diagram renders: `AZT Process Flow Chart.png` 2543x1796, `sorting_workflow_detailed.png` 3640x3520. The only photographic screenshot in docs/ is `docs/images/Python_path.png`, the Windows Python installer (referenced INSTALL.md:31, SIMPLEINSTALL.md:16).
  - _implication:_ Every screenshot in the new guide must be produced fresh. The icon art is still reusable as inline task glyphs (USAGE.md already does this), matching the app's own button imagery.

- **[high]** docs/sorting_workflow.svg already IS a screen-by-screen walkthrough — of vowel sorting only — and establishes a house style the new guide should extend rather than compete with.
  - _evidence:_ Text extracted from /Users/Seth/GIT/azt/docs/sorting_workflow.svg: 'A-Z+T — the vowel sorting process, screen by screen / Every box is a Run-Window screen or dialog; ↻ = the screen re-populates with the next item', then PHASE A words->sound-groups (SORT / VERIFY groups / JOIN groups) and PHASE B groups->letters (macrosort / VERIFY letters / JOIN letters / NAME-TRANSCRIBE letters). /Users/Seth/GIT/azt/docs/workflow.svg covers the whole pipeline in five stages with 'Add Words', 'Record Words', 'Sort Vowels/Consonants/Tone', 'Parse Words', 'Transcribe ...' boxes.
  - _implication:_ The wordlist-collection path has NO equivalent SVG. A cloud session can author `docs/word_collection_workflow.svg` in exactly this idiom unaided — no app run required — and that alone closes much of the 'what it does' half of Seth's ask.

- **[high]** The webview UI backend is not a viable shortcut to headless/HTML screenshots.
  - _evidence:_ /Users/Seth/GIT/azt/frontend/__init__.py:16-29 selects the backend from `AZT_UI_BACKEND`. /Users/Seth/GIT/azt/frontend/ui_webview.py header L11: 'Remaining widgets are stubs that log and no-op until Phase 4.' No `Text`/`TextBase` class exists in ui_webview (ui_tkinter has them at L1668/1731); `Notebook.add/select/bind` are `pass  # stub` (ui_webview.py:1019-1026); `ToolTip` is a 4-line no-op (L1391-1394). `pywebview` appears nowhere in /Users/Seth/GIT/azt/requirements.txt. pywebview also opens a NATIVE window (WKWebView/GTK-WebKit), so it still needs a display.
  - _implication:_ Plan on tkinter + a real (or virtual) display. Do not spend cloud time trying to render screens as HTML. (ui_webview.py:1541 `wait_window` IS implemented via threading.Event despite its stale 'Phase 5' docstring, and CLAUDE.md's completeness summary is optimistic — but the missing Text widget alone rules it out for the entry-field screens.)

- **[high]** Headless cloud screenshot capture is technically possible but practically the wrong tool: it needs Xvfb + python3-tk + portaudio + Charis SIL + a multi-GB dependency install + two git clones, and no such harness exists in the repo.
  - _evidence:_ Zero hits for xvfb/screenshot tooling: the only pyautogui references are commented out (/Users/Seth/GIT/azt/frontend/alphabet_chart.py:12 `# import pyautogui`, L33-43 commented `print_chart_screenshot`), and pyautogui is only in the pip_install backstop list (utilities/py_modules.py:~56), not requirements.txt. pytest.ini:11 declares `needs_display: requires a live Tk display (skipped in headless CI)` — the project explicitly treats display-requiring work as out of scope for headless runs. Linux extras: requirements.txt:10 `allosaurus; sys_platform == "linux"` (its editdistance C-extension is called out as a wheel problem), plus INSTALL.md:25 requires `python3-tk python3-pip portaudio19-dev` via apt.
  - _implication:_ Do not promise cloud screenshots. If Seth insists on trying, the sequence is: apt-get install xvfb python3-tk portaudio19-dev fonts-sil-charis imagemagick; git clone the repo + lift_templates; mkdir images/toselect; python3 -m venv env; pip install -r requirements.txt; xvfb-run -s '-screen 0 1400x1000x24' python main.py; capture with `import -window root`. Every one of those steps is a plausible failure point, and the three wait=True dialogs will block if any of font/sound/bootstrap is degraded.

- **[high]** Menus are hidden by default, so the guide must teach the right-click discovery gesture before any Advanced step can be followed.
  - _evidence:_ /Users/Seth/GIT/azt/docs/MENUS.md:3-5 '[A-Z+T] has by default **no menus** ... you can *show menus* through a context menu via right click anywhere in the main window –you may need to move the mouse around a bit before you right click.' docs/UI_GUIDE.md:31-34 repeats this ('I have deliberately hidden the menu'). docs/DEMOS.md:10-11 makes 'Make sure the menus are shown' a prerequisite for reaching the demo dialog from an open project.
  - _implication:_ Chapter 0 of the guide: right-click -> Show Menus, Show Buttons, smaller-font theme. Without it a reader cannot even reach 'Change to another Database' or the demo. This is also the single highest-value screenshot in the whole guide.

- **[high]** The docs describe a task set that is missing several tasks the current build ships, so the guide's 'what it does' section cannot be assembled from the docs alone.
  - _evidence:_ Present in code but absent from /Users/Seth/GIT/azt/docs/TASKS.md, TASKSCOLLECTION.md and TASKSANALYSIS.md: SortSyllables 'Sort Word Syllables' (tasks/tasks.py:1176, chooser.py:185), Parse/'Add and Parse Words' (tasks.py:557), BulkASR (tasks/bulk_asr.py:27, invoked chooser.py:437-438), AlphabetChart (tasks/alphabet_chart.py:8), AlphabetComparisonPages (tasks/alphabet_comparison.py:8), ExportData (tasks.py:31), and the LiftChooser options 'Get a project from your team (GitHub)' and 'Copy work from a USB drive' (ui_shell.py:3422-3423). Conversely /Users/Seth/GIT/azt/docs/ws/WORD_COLLECTION.md:6-17 documents Android 'A-Z+T Collab' and 'A-Z+T Recorder' apps and Bulk ASR that appear in no other doc and link out to a GitHub wiki page.
  - _implication:_ Build the task inventory by reading tasks/chooser.py:157-200 and the tasktitle strings, not the docs. Also flag to Seth: the phone Recorder apps and the wiki live outside this repo — a cloud session would need to fetch github.com/kent-rasmussen/azt/wiki/Apps to cover them (unverified content).

- **[high]** Word-collection screens need the CAWL illustration set to look like what users actually see, which is the one screenshot input that is genuinely expensive.
  - _evidence:_ Demo creation calls `demo.fill_db_images()` (ui_shell.py:3325) -> io_put/lift.py:2341 `fill_db_images` -> `sense.backfill_illustration()` (lift.py:3392), which returns False unless the per-sense directory under `images/toselect/` exists (lift.py:3407-3409). Directory naming per /Users/Seth/GIT/azt/images/to_select_ReadMe.md: 'image folders named by CAWL number (e.g. 0001_body/, 0002_skin_of_man/)'. docs/ws/WORD_COLLECTION.md:16 confirms pictures are central to the collection UX ('If you don't like the picture, click the circle in the picture to select another option').
  - _implication:_ If the guide is to show the collection screen honestly, images_CAWL must be cloned once on Seth's Mac. Budget the download deliberately; it is a one-time cost and it is also needed for the Alphabet Chart/Booklet outputs.

- **[low]** main.py's declared default task is one the chooser has commented out, a possible inconsistency worth checking before writing the guide's 'what you see first' text.
  - _evidence:_ /Users/Seth/GIT/azt/main.py:1156 `self.default_task='WordCollectnParse'` while tasks/chooser.py:180 has `# WordCollectnParse,` commented out of the datacollection list.
  - _implication:_ Whether a fresh project lands on 'Add and Parse Words' (non-audio) or 'Add and Parse Words with Audio' determines the first screenshot. Settle it by launching once, not by reading — I did not trace `default_task` through `makedefaulttask`.


## Open questions
- Does a fresh 'Start work on a new language' project land the user on 'Add and Parse Words with Audio' or on the commented-out non-audio variant? main.py:1156 sets default_task='WordCollectnParse' but chooser.py:180 comments that class out — resolve by launching once on the Mac, not by reading.
- Does the 'Add remaining CAWL entries' button actually vanish at runtime (IndexError swallowed at ui_shell.py:1103), or does some earlier gettext/translation path supply the {count} msgid instead? A single launch against a new empty project settles it, and it decides whether the guide can even show that button.
- Is Seth willing to install kivy into the system python 3.11 (fast path, AZT_NO_VENV=1) or does he want the full supported venv + requirements.txt install (multi-GB, downgrades nothing since it is isolated)? This choice gates everything else.
- How much bandwidth can be spent on the images_CAWL clone ('a few hundred MB' per sister_repos.py:59) — required for honest word-collection screenshots, suppressible with `mkdir images/toselect` if not.
- What does github.com/kent-rasmussen/azt/wiki/Apps say about the Android 'A-Z+T Collab' and 'A-Z+T Recorder' apps? docs/ws/WORD_COLLECTION.md makes them prerequisites for the word-collection workshop, but they are documented nowhere in the repo, and a phone-based collection guide would need entirely separate screenshots from a phone.
- Should the guide be authored as Markdown in docs/ (upstream-contributable, matches existing style, images committed to the repo) or as a published Artifact/PDF for Seth's Indonesian-speaking colleagues? Crowdin/gitlocalize localization exists for docs (docs/README.md:1-3, crowdin.yml), which argues for Markdown in docs/ with an eye to an ID translation.
- Does the cloud environment Seth intends to use actually have apt/root, ~5GB disk, and network to pypi/github? Nothing in this repo answers that, and it is the deciding factor for whether headless capture is even worth attempting.